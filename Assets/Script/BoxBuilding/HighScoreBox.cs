using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class HighScoreBox : MonoBehaviour
{
    /*
    //ScoreKeeper scoreHigh;
    
    [SerializeField] Text score;
    public int scoreMaxBox;
    
    void Awake()
    {

    }
    
     void Start() 
    {
        score.text = PlayerPrefs.GetInt("HighScoreBox").ToString();
        //score.text = scoreMaxBox.ToString();


    }
    
    void Update()
    {
        int number = GamePlayController.instance.scoreBox;
        int a = PlayerPrefs.GetInt("HighScoreBox",0);
        if(number > a)
        {
             PlayerPrefs.SetInt("HighScoreBox", number);
             score.text = number.ToString();
        }
    }
    */
    
}
